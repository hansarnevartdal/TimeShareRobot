<Page
    x:Class="TimeShareRobot.RobotBrain.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimeShareRobot.RobotBrain"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Width="Auto" Height="Auto">

    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="130" />
        </Grid.RowDefinitions>

        <Image Source="{Binding ControlQrc}"  Margin="0,10,10,0" Width="100" Height="100" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1"  Grid.Column="0" Grid.RowSpan="1" />
    </Grid>
</Page>
